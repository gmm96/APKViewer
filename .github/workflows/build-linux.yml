name: '[Linux] Build APKViewer'
on:
  push:
    tags:
      - 'v*'

jobs:
  build-linux:
    runs-on: ubuntu-22.04
    permissions:
      contents: write

    steps:
      - name: 'Checkout code'
        uses: actions/checkout@v4

      - name: 'Set up Python'
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 'Install system dependencies'
        run: |
          sudo apt-get update
          sudo apt-get install -y patchelf ccache

      - name: 'Install Python dependencies'
        run: |
          "$pythonLocation/bin/python" -m pip install --upgrade pip
          "$pythonLocation/bin/python" -m pip install -r requirements.txt
          "$pythonLocation/bin/python" -m pip install nuitka zstandard

      - name: 'Build executable with Nuitka'
        run: |
          "$pythonLocation/bin/python" -m nuitka \
              --standalone \
              --onefile \
              --output-dir=dist \
              --output-filename=APKViewer.bin \
              --include-package-data=androguard \
              --include-data-dir=assets=assets \
              --enable-plugin=tk-inter \
              --enable-plugin=anti-bloat \
              --nofollow-import-to=frida \
              --lto=yes \
              main.py

      - name: 'Upload binary to release'
        uses: softprops/action-gh-release@v2
        with:
          files: dist/APKViewer.bin
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
